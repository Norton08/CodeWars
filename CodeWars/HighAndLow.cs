using System;

public static class Kata
{
    public static string HighAndLow(string numbers)
    {
        int max = 0, min = 0;
        foreach (char c in numbers)
        {
            if (!c.Equals(" "))
            {
                if (c.)

            }
      
        }

        return $"{max} {min}";
    }
}